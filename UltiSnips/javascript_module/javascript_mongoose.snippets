snippet mongo ""
import mongoose from 'mongoose';

mongoose.Promise = global.Promise;
const url = ${1};
const db = mongoose.createConnection(url);
const tableName = ${2};
const {3} = db.model(tableName, mongoose.Schema({
  _id: String,
  ${4}
}, {
  versionKey: false,
}), tableName);
export default Users;
endsnippet
